import { Lusitana, Lexend } from 'next/font/google';

export const lusitana = Lusitana({ subsets: ['latin'], weight: '400' });
export const lexend = Lexend({
  subsets: ['latin'],
  weight: ['400', '500', '600', '700'],
});
